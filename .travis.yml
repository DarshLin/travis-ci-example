language: java
sudo: true
dist: bionic
jdk:
  - oraclejdk12

before_install:
  - chmod +x mvnw

script: ./mvnw clean install

jobs:
  include:
    -stage: Test
    script: ./mvnw clean test

notifications:
  email: darshlin714@gmail.com